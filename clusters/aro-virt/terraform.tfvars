# Two-checkout attach: installer profile clusters/aro-virt.
# Set ARO_HCP_ROOT to the installer checkout (Make refreshes platform.json), or
# point platform_json at that profile's gitignored platform.json.
platform_json = "../../../clusters/aro-virt/platform.json"

# pool_size_tib = 1
